version: "3.8"
services:
  muscle-tracker:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - DATABASE_PATH=/app/data/training.db
